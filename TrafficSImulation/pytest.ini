[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -m "not live" --ignore=tests/test_google_startup.py
markers =
    live: requires live external credentials and is excluded from the default test run
